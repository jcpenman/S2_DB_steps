CREATE TABLE RESET_COURSE
(
   CRSE_ID        NUMBER (9) NOT NULL,
   SESSION_CODE   NUMBER (4),
   CREATED_DATE   DATE DEFAULT SYSDATE
)
TABLESPACE SLC_DATA
RESULT_CACHE (MODE DEFAULT)
LOGGING;

GRANT DELETE,
      INSERT,
      SELECT,
      UPDATE
   ON RESET_COURSE
   TO SGAS;