CREATE TABLE RESET_INST_TERM
(
  INST_CODE     VARCHAR2(5 ) NOT NULL,
  SESSION_CODE  NUMBER(4)  NOT NULL,
  TERM_NO       NUMBER(2)  NOT NULL,
   CREATED_DATE   DATE DEFAULT SYSDATE
)
TABLESPACE SLC_DATA
RESULT_CACHE (MODE DEFAULT)
LOGGING;

GRANT DELETE,
      INSERT,
      SELECT,
      UPDATE
   ON RESET_INST_TERM
   TO SGAS;